export interface StudentData {
    id: string;
    name: string;
    role: string;
    birthDate: string;
    gender: string;
    email: string;
    currentSemesterCredits: number;
    totalCredits: number;
    maxCredits: number
}

